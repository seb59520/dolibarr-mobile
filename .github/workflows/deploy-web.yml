name: Deploy web bundle

on:
  push:
    branches: [main]
    paths:
      - 'lib/**'
      - 'web/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'assets/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

concurrency:
  group: deploy-web
  cancel-in-progress: false

jobs:
  build-and-deploy:
    name: Build Flutter web + rsync to nginx
    runs-on: [self-hosted, docker-srv, flutter]
    timeout-minutes: 30

    steps:
      - name: Pre-checkout — chown workspace to runner
        # Le container flutter-ci tourne en root et génère .dart_tool/, build/,
        # ios/Flutter/ephemeral/ avec ownership root → actions/checkout (qui
        # tourne en gh-runner) ne peut pas les nettoyer. On force le chown
        # avant le checkout pour que le nettoyage passe.
        run: docker exec flutter-ci chown -R "$(id -u):$(id -g)" /workspace 2>/dev/null || true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Pub get
        run: docker exec -w /workspace flutter-ci flutter pub get

      - name: Build runner (Drift / Freezed / JSON)
        run: docker exec -w /workspace flutter-ci dart run build_runner build --delete-conflicting-outputs

      - name: Flutter analyze
        run: docker exec -w /workspace flutter-ci flutter analyze

      - name: Flutter test
        run: docker exec -w /workspace flutter-ci flutter test --reporter expanded --exclude-tags=golden

      - name: Flutter build web (release)
        run: docker exec -w /workspace flutter-ci flutter build web --release --no-tree-shake-icons

      - name: Deploy via rsync to nginx html
        run: |
          rsync -a --delete \
            /opt/actions-runner-dolibarr-mobile/_work/dolibarr-mobile/dolibarr-mobile/build/web/ \
            /opt/docker/dolibarr-mobile-web/html/
          echo "Deployed at $(date --iso-8601=seconds)"

      - name: Post-run — chown workspace back to runner
        # Double sécurité : même si la build a planté, on remet l'ownership
        # pour ne pas bloquer le prochain checkout.
        if: always()
        run: docker exec flutter-ci chown -R "$(id -u):$(id -g)" /workspace 2>/dev/null || true

      - name: Summary
        if: always()
        run: |
          echo "## Deploy summary" >> $GITHUB_STEP_SUMMARY
          echo "- Commit : \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- URL    : https://dolibarr-mobile.lab.scinnova-academy.cloud" >> $GITHUB_STEP_SUMMARY
          echo "- Time   : $(date --iso-8601=seconds)" >> $GITHUB_STEP_SUMMARY
